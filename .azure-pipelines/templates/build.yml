parameters:
  - name: dockerImage
    type: string
  - name: jarName
    type: string

jobs:
  - job: Build
    displayName: Build with Docker
    pool:
      vmImage: 'ubuntu-latest'
    steps:
      - script: |
          docker run --rm -v $(pwd):/app --workdir /app ${{ parameters.dockerImage }} bash -c "apt-get update && apt-get install -y tar curl && ./mvnw clean package -DskipTests"
        displayName: 'Build JAR with Docker'
      - script: |
          ls -lh target/
        displayName: 'List build artifacts'
      - script: |
          echo "##vso[task.setvariable variable=JAR_PATH]$(pwd)/target/${{ parameters.jarName }}"
        displayName: 'Set JAR path variable'